---
import Icon from "./Icon.astro";
import SupportModal from "./SupportModal.astro";
import {
  languageOptions,
  localizedPath,
  localePaths,
  type CoffeeSupport,
  type Locale,
  type NavItem
} from "../data/site";

interface Props {
  brand: string;
  links: NavItem[];
  connect: string;
  languageLabel: string;
  openMenuLabel: string;
  closeMenuLabel: string;
  locale: Locale;
  coffeeSupport: CoffeeSupport;
}

const {
  brand,
  links,
  connect,
  languageLabel,
  openMenuLabel,
  closeMenuLabel,
  locale,
  coffeeSupport
} = Astro.props;
const resolveHref = (href: string) => href.startsWith("#") ? `${localizedPath(locale)}${href}` : href;
const cvPaths: Record<Locale, string> = {
  en: "/cv/jose-manuel-cortes-cv(english).pdf",
  es: "/cv/jose-manuel-cortes-cv(spanish).pdf",
  "zh-cn": "/cv/jose-manuel-cortes-cv(cn).pdf"
};
const mobileLinks = [
  ...links,
  { label: "GitHub", href: "https://github.com/deepdevjose" },
  { label: "LinkedIn", href: "https://www.linkedin.com/in/deepdevjose/" },
  { label: locale === "es" ? "Descargar CV" : locale === "zh-cn" ? "下载简历" : "Download CV", href: cvPaths[locale] },
  { label: connect, href: "#contact" }
];
---

<header class="fixed inset-x-0 top-0 z-50 border-b border-black/5 bg-paper/86 backdrop-blur-xl backdrop-saturate-150">
  <nav class="mx-auto max-w-site px-5 lg:px-8" aria-label="Primary navigation">
    <div class="flex h-16 items-center justify-between gap-4">
      <a href={localizedPath(locale)} class="site-brand flex items-center font-semibold text-ink transition hover:-translate-y-0.5 hover:text-accent">
        <span>{brand}</span>
      </a>

      <div class="hidden items-center gap-7 md:flex">
        {links.map((item) => (
          <a class="rounded-md px-2 py-1 text-sm font-medium text-ink/78 transition hover:bg-black/4 hover:text-accent" href={resolveHref(item.href)}>
            {item.label}
          </a>
        ))}
      </div>

      <div class="flex items-center gap-6">
        <div class="hidden h-9 items-center gap-4 sm:flex" aria-label={languageLabel}>
          {languageOptions.map((option) => (
            <a
              href={localePaths[option.locale]}
              class:list={[
                "relative inline-flex h-8 items-center rounded-md px-2 text-xs leading-none transition",
                option.locale === locale ? "font-bold text-ink after:absolute after:bottom-0 after:left-1/2 after:h-1 after:w-1 after:-translate-x-1/2 after:rounded-full after:bg-accent" : "font-semibold text-muted hover:bg-black/4 hover:text-accent"
              ]}
              aria-current={option.locale === locale ? "page" : undefined}
              title={option.name}
            >
              {option.label}
            </a>
          ))}
        </div>

        <a
          href={`${localizedPath(locale)}#contact`}
          class="group hidden items-center gap-2 rounded-lg bg-ink px-4 py-2.5 text-sm font-semibold text-white shadow-sm transition hover:-translate-y-0.5 hover:bg-accent hover:shadow-md md:inline-flex"
        >
          <span>{connect}</span>
          <Icon name="arrow-up-right" class="h-4 w-4 transition group-hover:translate-x-0.5 group-hover:-translate-y-0.5" />
        </a>

        <button
          type="button"
          class="group hidden items-center gap-2 rounded-lg border border-black/10 bg-transparent px-4 py-2.5 text-sm font-semibold text-muted transition hover:-translate-y-0.5 hover:border-accent hover:bg-[rgba(44,60,93,0.08)] hover:text-accent hover:shadow-sm md:inline-flex"
          aria-label={coffeeSupport.ariaLabel}
          aria-haspopup="dialog"
          data-support-trigger
        >
          <svg class="h-4 w-4 shrink-0 text-accent transition group-hover:text-ink" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M7 8h9a3 3 0 0 1 0 6h-1" />
            <path d="M6 8h11v7a4 4 0 0 1-4 4H10a4 4 0 0 1-4-4V8Z" />
            <path d="M8 5v3M12 5v3M16 5v3" />
            <path d="M4 19h16" />
          </svg>
          <span>{coffeeSupport.tooltipLabel}</span>
        </button>
        <SupportModal {...coffeeSupport} titleId="support-modal-title-navbar" />

        <button
          id="mobile-menu-button"
          aria-controls="mobile-menu"
          aria-expanded="false"
          class="inline-flex h-10 w-10 items-center justify-center rounded-lg border border-black/5 bg-white/90 p-0 text-ink md:hidden"
          aria-label={openMenuLabel}
        >
          <svg class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 12h18M3 6h18M3 18h18"/></svg>
        </button>
      </div>
    </div>

    <div id="mobile-menu" class="hidden fixed inset-0 z-[80] h-dvh w-screen flex-col overflow-y-auto bg-paper p-5 md:hidden" aria-hidden="true">
      <div class="flex items-center justify-between">
        <a href={localizedPath(locale)} class="text-xl font-semibold">{brand}</a>
        <button id="mobile-menu-close" class="inline-flex h-10 w-10 items-center justify-center rounded-lg border border-black/5 bg-white p-0 text-ink transition hover:bg-[rgba(44,60,93,0.08)] hover:text-accent" aria-label={closeMenuLabel}>
          <svg class="h-6 w-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6L6 18M6 6l12 12"/></svg>
        </button>
      </div>

      <nav class="mt-8 flex flex-col gap-3" aria-label="Mobile primary">
        {mobileLinks.map((item) => (
          <a
            href={resolveHref(item.href)}
            target={item.href.startsWith("http") ? "_blank" : undefined}
            rel={item.href.startsWith("http") ? "noreferrer" : undefined}
            download={item.href.endsWith(".pdf") ? true : undefined}
            class:list={[
              "flex min-h-11 items-center justify-between rounded-lg px-4 py-3 text-lg font-semibold transition",
              item.href === "#contact" ? "bg-ink text-white shadow-sm hover:bg-accent" : "text-ink hover:bg-[rgba(44,60,93,0.08)] hover:text-accent"
            ]}
          >
            <span>{item.label}</span>
            <Icon
              name={item.href.endsWith(".pdf") ? "download" : item.href.startsWith("http") ? "arrow-up-right" : "arrow-down"}
              class:list={["h-4 w-4", !item.href.startsWith("http") && !item.href.endsWith(".pdf") ? "-rotate-90" : ""]}
            />
          </a>
        ))}

        <button
          type="button"
          class="flex min-h-11 items-center justify-between rounded-lg border border-black/10 bg-transparent px-4 py-3 text-lg font-semibold text-muted transition hover:border-accent hover:bg-[rgba(44,60,93,0.08)] hover:text-accent"
          aria-label={coffeeSupport.ariaLabel}
          aria-haspopup="dialog"
          data-support-trigger
        >
          <span class="flex items-center gap-2">
            <svg class="h-4 w-4 shrink-0 text-accent" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M7 8h9a3 3 0 0 1 0 6h-1" />
              <path d="M6 8h11v7a4 4 0 0 1-4 4H10a4 4 0 0 1-4-4V8Z" />
              <path d="M8 5v3M12 5v3M16 5v3" />
              <path d="M4 19h16" />
            </svg>
            <span>{coffeeSupport.tooltipLabel}</span>
          </span>
          <Icon name="arrow-up-right" class="h-4 w-4" />
        </button>
        <SupportModal {...coffeeSupport} titleId="support-modal-title-navbar-mobile" />

        <div class="mt-4 border-t border-black/6 pt-4">
          <p class="mb-2 text-sm text-muted">{languageLabel}</p>
          <div class="flex gap-2">
            {languageOptions.map((option) => (
              <a
                href={localePaths[option.locale]}
                class:list={[
                  "rounded-md px-3 py-2 text-sm font-semibold transition",
                  option.locale === locale ? "bg-ink text-white" : "text-muted hover:bg-[rgba(44,60,93,0.08)] hover:text-accent"
                ]}
                aria-current={option.locale === locale ? "page" : undefined}
              >
                {option.label}
              </a>
            ))}
          </div>
        </div>
      </nav>
    </div>

    <script>
      (function(){
        const btn = document.getElementById('mobile-menu-button');
        const close = document.getElementById('mobile-menu-close');
        const menu = document.getElementById('mobile-menu');
        if(!btn || !menu) return;
        const mobileMenu = menu!;
        const mobileMenuButton = btn!;
        function open(){
          mobileMenu.classList.remove('hidden');
          mobileMenu.setAttribute('aria-hidden','false');
          mobileMenuButton.setAttribute('aria-expanded','true');
          document.body.style.overflow='hidden';
        }
        function closeMenu(){
          mobileMenu.classList.add('hidden');
          mobileMenu.setAttribute('aria-hidden','true');
          mobileMenuButton.setAttribute('aria-expanded','false');
          document.body.style.overflow='';
        }
        mobileMenuButton.addEventListener('click', open);
        if(close) close.addEventListener('click', closeMenu);
        mobileMenu.addEventListener('click', (e)=>{ if(e.target===mobileMenu) closeMenu(); });
        mobileMenu.querySelectorAll('a').forEach((link)=>link.addEventListener('click', closeMenu));
      })();
    </script>
  </nav>
</header>
